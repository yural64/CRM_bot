# 📘 Инструкция по установке и настройке

Пошаговое руководство для запуска CRM Telegram бота.

## 🔧 Требования

- Python 3.10 или выше
- Telegram аккаунт
- Доступ в интернет

## 📥 Шаг 1: Установка Python

### Windows
1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке отметьте "Add Python to PATH"
3. Проверьте установку:
```bash
python --version
```

### Linux/macOS
Python обычно предустановлен. Проверьте:
```bash
python3 --version
```

## 🤖 Шаг 2: Создание Telegram бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Введите имя бота (например: "My CRM Bot")
5. Введите username бота (должен заканчиваться на `bot`, например: `my_crm_bot`)
6. **Сохраните токен** — он понадобится позже!

Пример токена: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`

## 🗄 Шаг 3: Создание базы данных Neon

1. Перейдите на [neon.tech](https://neon.tech)
2. Нажмите **Sign up** (регистрация через GitHub/Google)
3. Создайте новый проект:
   - Project name: `crm_bot`
   - Region: выберите ближайший
   - PostgreSQL version: последняя
4. Нажмите **Create Project**
5. Скопируйте **Connection string** (строку подключения)

Пример: `postgresql://username:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require`

## 💻 Шаг 4: Установка проекта

### 1. Скачайте или клонируйте проект

```bash
cd путь/к/папке/CRM_Bot
```

### 2. Создайте виртуальное окружение (рекомендуется)

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

## ⚙️ Шаг 5: Настройка

### 1. Создайте файл `.env`

Скопируйте `env.example.txt` как `.env`:

**Windows:**
```bash
copy env.example.txt .env
```

**Linux/macOS:**
```bash
cp env.example.txt .env
```

### 2. Заполните `.env`

Откройте файл `.env` в текстовом редакторе и вставьте свои данные:

```env
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
DATABASE_URL=postgresql://username:password@ep-cool-name-123456.us-east-2.aws.neon.tech/neondb?sslmode=require
```

## 🎯 Шаг 6: Первый запуск

```bash
python bot.py
```

Вы должны увидеть:
```
🤖 CRM-бот запускается...
✅ Таблицы базы данных готовы
✅ Подключение к базе данных успешно
📊 Клиентов: 0
📊 Услуг: 0
📊 Записей: 0
🚀 Бот запущен и готов к работе!
```

## ➕ Шаг 7: Добавление услуг

### Вариант 1: Через Neon Dashboard

1. Откройте [console.neon.tech](https://console.neon.tech)
2. Выберите свой проект
3. Перейдите в **SQL Editor**
4. Вставьте и выполните:

```sql
INSERT INTO services (name, price, duration_minutes, description)
VALUES 
    ('Консультация', 1000.00, 30, 'Первичная консультация'),
    ('Базовая услуга', 2000.00, 60, 'Основная услуга'),
    ('Премиум услуга', 5000.00, 120, 'Расширенная услуга');
```

### Вариант 2: Через Python скрипт

Создайте файл `add_services.py`:

```python
from database import db

services = [
    ("Стрижка", 1500.00, 60, "Мужская или женская стрижка"),
    ("Окрашивание", 3000.00, 120, "Окрашивание волос"),
    ("Маникюр", 2000.00, 90, "Классический маникюр"),
]

for name, price, duration, description in services:
    service_id = db.add_service(name, price, duration, description)
    print(f"✅ Добавлена услуга: {name} (ID: {service_id})")
```

Запустите:
```bash
python add_services.py
```

## ✅ Шаг 8: Тестирование

1. Откройте Telegram
2. Найдите своего бота по username
3. Нажмите **Start**
4. Попробуйте команды:
   - `/register` — регистрация
   - `/services` — список услуг
   - `/book` — запись на услугу

## 🔍 Решение проблем

### Бот не отвечает

1. Проверьте, запущен ли `bot.py`
2. Проверьте правильность `BOT_TOKEN` в `.env`
3. Проверьте интернет-соединение

### Ошибка подключения к БД

1. Проверьте `DATABASE_URL` в `.env`
2. Убедитесь, что строка подключения правильная
3. Проверьте, что проект Neon активен (не приостановлен)

### Ошибка "No module named 'telebot'"

```bash
pip install -r requirements.txt
```

### Ошибка "No module named 'dotenv'"

```bash
pip install python-dotenv
```

## 🚀 Запуск в продакшене

### Linux (systemd)

Создайте файл `/etc/systemd/system/crm-bot.service`:

```ini
[Unit]
Description=CRM Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/CRM_Bot
ExecStart=/path/to/venv/bin/python bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable crm-bot
sudo systemctl start crm-bot
sudo systemctl status crm-bot
```

### Windows (Task Scheduler)

1. Откройте **Task Scheduler**
2. **Create Basic Task**
3. Trigger: **At startup**
4. Action: **Start a program**
5. Program: `C:\path\to\venv\Scripts\python.exe`
6. Arguments: `C:\path\to\CRM_Bot\bot.py`

## 📊 Мониторинг

Логи бота выводятся в консоль. Для продакшена используйте:

```bash
python bot.py >> bot.log 2>&1
```

## 🎉 Готово!

Ваш CRM-бот готов к работе! 

Для вопросов и поддержки обратитесь к документации или создайте issue.

